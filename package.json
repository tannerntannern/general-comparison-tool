{
    "private": true,
    "scripts": {
        "test": "tsc --noEmit",
        "dev": "esbuild src/index.tsx --servedir=src --bundle --inject:./react-shim.js",
        "build": "rimraf dist && mkdir dist && cpy --flat src/index.html dist && esbuild src/index.tsx --bundle --minify --outdir=dist --inject:./react-shim.js"
    },
    "dependencies": {
        "@emotion/react": "^11.8.2",
        "@emotion/styled": "^11.8.1",
        "@mui/icons-material": "^5.5.1",
        "@mui/lab": "^5.0.0-alpha.75",
        "@mui/material": "^5.5.3",
        "react": "^17",
        "react-dom": "^17",
        "react-use": "^17.3.2"
    },
    "devDependencies": {
        "@types/react": "^17.0.43",
        "@types/react-dom": "^17.0.14",
        "cpy-cli": "^4.1.0",
        "esbuild": "^0.14.31",
        "rimraf": "^3.0.2",
        "typescript": "^4.6.3"
    }
}
